/**
 * Wraps async route handlers to catch errors and pass them to error handler
 * This is a re-export from the shared middleware for consistency
 */
module.exports = require('../../middleware/asyncHandler');


